import SwiftUI
struct MSoundTheme {
    static let accentColor = Color(red: 0.1, green: 0.8, blue: 0.5)
    static let backgroundColor = Color(red: 0.05, green: 0.05, blue: 0.05)
    static let secondaryBackground = Color(red: 0.12, green: 0.12, blue: 0.12)
    static let textColor = Color.white
    static let secondaryTextColor = Color.gray
}