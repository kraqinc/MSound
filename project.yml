name: MSound
options:
  bundleIdPrefix: com.kalanba
targets:
  MSound:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: 
      - path: .
        include: ["*.swift", "Info.plist"]
    settings:
      base:
        INFOPLIST_FILE: Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.kalanba.msound
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
